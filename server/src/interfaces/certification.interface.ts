export interface certificationType {
  name? : string;
  mail?: string;
  impUid?: string;
}

export interface certificationInfo {
  name: string;
  gender: string;
  birth: string;
  userDI: string;
}
