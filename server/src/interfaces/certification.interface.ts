export interface CertificationType {
  name? : string;
  mail?: string;
  impUid?: string;
}

export interface CertificationInfo {
  name: string;
  gender: string;
  birth: string;
  userDI: string;
}

export interface CheckIdType {
  userId? : string,
  impUid? : string,
}
