import { makeStyles } from '@material-ui/core';

const useSiedebarStyles = makeStyles((theme) => ({
  // styles here
  // check this documentation
  // https://material-ui.com/styles/api/#makestyles-styles-options-hook
}));

export default useSiedebarStyles;
