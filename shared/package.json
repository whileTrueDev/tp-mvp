{
  "name": "@truepoint/shared",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "start": "tsc --watch",
    "start:dev": "tsc --watch",
    "lint": "eslint . --ext .js,.jsx,.ts,.tsx --ignore-pattern '/dist/'",
    "lint:fix": "eslint . --ext .js,.jsx,.ts,.tsx --fix --ignore-pattern '/dist/'",
    "lint:commit:fix": "eslint --fix"
  },
  "dependencies": {
    "class-validator": "^0.12.2"
  }
}